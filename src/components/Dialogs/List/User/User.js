import React from 'react'
import classes from './User.module.css'

const User = () => {
    return (
        <form className={classes.main}>
            user name <br/>
            user last message
        </form>
    )
}

export default User
